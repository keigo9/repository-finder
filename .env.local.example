# GitHub API 認証トークン（任意）
# Personal Access Token を設定することでレート制限が 60 req/hour → 5000 req/hour に向上
# https://github.com/settings/tokens から "classic token" を生成
# スコープ: public_repo (または repo:public_repositories) のみでOK
GITHUB_TOKEN=
